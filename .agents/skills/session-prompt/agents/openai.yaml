interface:
  display_name: "session-prompt"
  short_description: "Start or resume a CKC UI work session from a task argument."
  default_prompt: "$session-prompt <TASK>"

policy:
  allow_implicit_invocation: false
