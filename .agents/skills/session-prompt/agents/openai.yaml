interface:
  display_name: "session-prompt"
  short_description: "Start a CKC manuscript-figures session from a task."
  default_prompt: "$session-prompt [TASK]"

policy:
  allow_implicit_invocation: false
