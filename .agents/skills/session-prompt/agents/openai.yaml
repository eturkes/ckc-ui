interface:
  display_name: "session-prompt"
  short_description: "Start a CKC UI session from a task or advance the project session plan."
  default_prompt: "$session-prompt [TASK]"

policy:
  allow_implicit_invocation: false
