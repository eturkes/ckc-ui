# Reusable Session Prompt

Preferred invocation:
`$session-prompt [TASK]`

You are working in `/run/host/home/eturkes/Projects/scratch/ckc-ui`.

Load order:
1. `AGENTS.md`
2. `.agent/session-plan.md` when `$session-prompt` has no task text
3. `SPEC.md` sections needed for the requested unit
4. `.agent/memory.md` if it exists

Task selection:
- If `<TASK>` is non-empty, use it as the requested task.
- If `<TASK>` is empty, read `.agent/session-plan.md`, select the first queue item whose `status` is `pending`, and treat that item title, intent, deliverables, and gate as the requested task.
- If every queue item is `done`, report exactly that the CKC realism session plan is complete and make no file changes.
- If the plan is missing or unparseable, ask for the missing task or plan before changing files.

Standing project intent: CKC is a headless clinical knowledge compiler. UI work must present research evidence, traceability, replay, artifacts, metrics, ledgers, and gates. It must avoid clinical decision-support claims unless explicit gate evidence exists.

Current seed artifact: root `index.html` is generated by `tools/build-run.mjs` from the ignored `runs/m2-one-shot/` evidence bundle. Treat it as a one-shot fixture-scale M1-M2 research harness view, not the normative compiler architecture.

Plan-mode closeout:
1. Run the selected item's gate.
2. If the gate passes and the item is complete, update `.agent/session-plan.md` by changing only that item's status to `done`.
3. If the gate does not pass, leave the item `pending`, report the blocker, and do not start the next item.
4. Update `.agent/memory.md` only for durable lessons future sessions need.

Default closeout:
1. Verify changed artifacts with local tools.
2. Keep `.gitignore` aligned with generated outputs.
3. Commit one scoped change before final response when work is complete.
