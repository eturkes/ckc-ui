# Agent Memory

- Current project focus: no browser UI. Work from canonical run artifacts and tracked manuscript figures under `figures/manuscript/`; root `index.html` is obsolete, ignored, and should not be regenerated unless the user explicitly restores UI work.
- `tools/build-run.mjs` is a deliberate JS-only deviation from spec04's Rust/model/solver stack. It regenerates ignored `runs/m2-one-shot/`, writes reports/metrics/trace/replay artifacts, and asserts one M1 contradiction plus one documented null result. Treat it as fixture-scale demo evidence, not the normative compiler architecture.
- Current M2 one-shot route target: `route.direct_smt` model output is SMT-LIB; `route.single_ir` model output is bounded cue JSON, bridged into contrived `schema.route_rule_ir.v0`, then deterministically compiled to per-row SMT-LIB under `runs/m2-one-shot/route_targets/route.single_ir/`. `metrics/route_targets.json` is the durable route-target summary.
- 2026-06-11 R3 less-leaky M2 rerun: `route.single_ir` prompts no longer include the filled JSON answer object or per-label normalized field payloads. M2 scoring uses 3 groups (`group.m1_conflict`, `group.m1_null`, `group.m2_holdout_conflict`) x 3 seeds x 2 routes = 18 live calls, with `metrics/route_evaluation.json` documenting `source_derived_route_pair_evaluator.v2`. The harder R3 live run is a scaffolded cue-translation null result for lift: direct SMT admitted accuracy `0/9`; single-IR admitted accuracy `0/9`; single-IR candidate accuracy `3/9`.
- Default local model for live verification is project-local `llama.cpp` with `Qwen2.5-0.5B-Instruct-Q2_K` (`sha256 9ee36184e616dfc7...`) in ignored `.local/`. `npm run verify` runs live model calls; `npm run verify:recorded` is unsupported-model fallback evidence and changes ignored run outputs.
- Prompt provenance convention: `tools/build-run.mjs` writes exact LLM prompt text to `runs/m2-one-shot/prompts/**/prompt-<hash>.txt`, indexes prompts in `runs/m2-one-shot/prompts/catalog.json`, and adds `prompt_hash` to each `model_io/**` record. Use this catalog for prompt audits before scanning all model I/O files.
- Model-facing prompt realism: prompts passed to LLMs should frame tasks as hospital CDS knowledge-base maintenance/import or formalization tickets. Keep route IDs, research-harness language, weak-model labels, synthetic-fixture labels, and measurement mechanics in artifacts/reports, not in the prompt text sent through `llama.cpp -p`.
- 2026-06-11 R1 data spine: fixture regions, terminology bindings, clinical statements, and NormRule specs live in `corpus/fixtures/m1_fixture_semantics.json`; M1 group membership comes from `registry/experiments.json`; expected outcomes come from `corpus/gold/m1_expected.json`. Keep future fixture semantics data-driven there instead of adding `a`/`b`/`control` branches to `tools/build-run.mjs`.
- 2026-06-11 R2 real-source candidate path: `tools/build-run.mjs` emits candidate-only real guideline artifacts under ignored `runs/m2-one-shot/real_guidelines/artifacts/<source-id>/{source_graph,segments,normalization,route_rule_ir}.json` from `corpus/real_guidelines/japanese_guidelines.json`. These rows are `source_intake_candidate_only`, outside locked M1/M2 scoring; registry `machine_hint` values are retained as author-provided residual evidence, with unsupported/missing/ambiguous toy-schema fields blocking candidate rule admission.
- `corpus/real_guidelines/japanese_guidelines.json` is the committed source/permission/span registry; `npm run fetch:guidelines` downloads raw J-STAGE HTML/PDF bytes into ignored `corpus/raw/real-guidelines/`.
- Manuscript figures: `tools/build-figures.mjs` prefers `runs/m3-routes/report.json` when present and falls back to `runs/m2-one-shot/report.json`; it writes tracked ArXiv-oriented SVG/PDF pairs plus `figures.tex`, `README.md`, `manifest.json`, and deterministic multi-page `manuscript_figures.pdf` under `figures/manuscript/`. Rebuild figures after route-report regeneration and check `figures/manuscript/manifest.json.source_report_path` before interpreting captions.
- `npm run verify:figures` checks the figure bundle is a PDF and has one `/Page` object per figure.
- Keep operating instructions grounded in tools and files actually present in this repository. Avoid carrying over tool-specific directories, fixed context-window protocols, or missing maintenance scripts unless the repo later adds them explicitly.
- Codex custom prompts under `~/.codex/prompts` are user-global. Project-local command-like workflows belong under `.agents/skills`; `$session-prompt <TASK>` is a repo skill and parses the task text from the invoking message.
- 2026-06-11 comparison expansion queue: `.agent/session-plan.md` is active. No-argument `$session-prompt` selects the first queue item whose `status` is not `done`; after C5 completion, the next item is C6 M3 fixture and mutation expansion. The old R1-R3 realism queue has been replaced rather than retained in the active plan.
- 2026-06-11 C1 route-matrix migration: `tools/build-run.mjs` now emits `metrics/route_matrix.json` / `report.metrics.route_matrix` instead of the fixed `lift_table`; `route.direct_smt` is the explicit baseline. `metrics/route_targets.json` / `report.route_target_summary` is schema `route_target_summary.v1` with `routes[]`, `total_compiled_row_count`, and `total_smt_file_count`; future routes should add compiled targets by route, not by replacing a single `route.single_ir` summary. C1 gates passed with `npm run verify:recorded`, `npm run verify` (to restore live R3 figure evidence), `npm run build:figures`, and `npm run verify:figures`.
- 2026-06-11 C2 scaffold: `tools/build-run.mjs` defaults to `exp.m2_lift`; select M3 with `--experiment exp.m3_routes`. Any registered route not listed in `implementedRouteIds` fails closed unless `--scaffold-routes` is explicit. In scaffold mode it emits `measurement_status: scaffold_closed_unimplemented`, `model_call_recorded: false`, `deferred_gate_required`, zero prompt-catalog calls, and no subprocess. C2 gates passed with `npm run verify:recorded`, `node tools/build-run.mjs --recorded-model --print-config`, `node tools/build-run.mjs --verify --recorded-model --experiment exp.m3_routes --scaffold-routes`, a JSON scaffold audit over `runs/m3-routes/report.json`, `npm run verify`, `npm run build:figures`, and `npm run verify:figures`.
- 2026-06-11 C3 stacked route: `route.stacked_ir` is implemented in `tools/build-run.mjs` as one schema-constrained model call per source pair using `schema.stacked_ir.v0` (`source_frame -> rule_row`) and deterministic `stacked_ir_v0_to_route_rule_ir_v0` bridge into the existing `route_rule_ir_v0_to_smt_v0` compiler. C3 live `exp.m3_routes --scaffold-routes` verification had model-output syntax `9/9`, target syntax `9/9`, candidate accuracy `3/9`, admitted accuracy `0/9`, compiled rows `9`, SMT files `12`, and bridge schema `schema.stacked_ir.v0`.
- 2026-06-11 C4 hop-chain route: `route.ir_hop_chain` is implemented in `tools/build-run.mjs` as three adjacent constrained JSON calls per route row: lexical cues (`schema.ir_hop_chain.lexical_cues.v0`) -> clinical frame (`schema.ir_hop_chain.clinical_frame.v0`) -> rule rows (`schema.ir_hop_chain.rule_rows.v0`), bridged by `ir_hop_chain_v0_to_route_rule_ir_v0` into the existing SMT compiler. C4 live `exp.m3_routes --scaffold-routes` verification had model-output syntax `9/9`, target syntax `9/9`, candidate accuracy `9/9`, admitted accuracy `0/9`, compiled rows `9`, SMT files `15`, `model_call_count` `27`, and hop lineage on every model_io row. Admission was blocked by grounding/bridge residuals, so this was target-syntax/candidate-verdict evidence only.
- 2026-06-11 C5 CKC-layered route: `route.ckc_layered` is implemented in `tools/build-run.mjs` as three CKC-native constrained JSON stages per route row: segments (`schema.ckc_layered.segments.v0`) -> statements (`schema.ckc_layered.statements.v0`) -> rules (`schema.ckc_layered.rules.v0`), bridged by `ckc_layered_v0_to_route_rule_ir_v0` into the existing SMT compiler with stage lineage in `model_io`. Live `node tools/build-run.mjs --verify --live-model --experiment exp.m3_routes` passed with 81 total model calls and no scaffold rows; CKC-layered rows had model-output syntax `9/9`, target syntax `0/9`, candidate accuracy `0/9`, admitted accuracy `0/9`, compiled target records `9`, SMT files `0`, and `model_call_count` `27`. C5 manuscript figures now source `runs/m3-routes/report.json` and show all five routes.
- 2026-06-11 C6 fixture expansion: `exp.m3_routes` now has 8 groups: 2 original M1, 1 M2 holdout, 2 M3 metamorphic, and 3 M3 expanded groups (`numeric_threshold_empty_intersection`, `strict_factual_contradiction`, `terminology_incoherence`). Six new committed synthetic fixtures use labels A2/B2/E/F/G/H; G/H intentionally carry both strict factual contradiction evidence and a terminology binding collision. `tools/build-run.mjs` emits `metrics/group_audit.json` / `report.m3_group_audit`, and the direct registry audit must show every M3 group has gold, fixture semantics, source paths, and evidence regions. Live `node tools/build-run.mjs --verify --live-model --experiment exp.m3_routes` passed with 216 calls and candidate accuracy direct `0/24`, single_ir `6/24`, stacked_ir `6/24`, ir_hop_chain `9/24`, ckc_layered `0/24`; admitted accuracy remained `0/24` for every route.
