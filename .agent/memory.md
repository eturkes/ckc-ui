# Agent Memory

- CKC UI framing: present a research/evidence workbench over canonical artifacts, not a clinical UI. Primary surfaces are run state, findings/null results, lineage from source span to solver verdict, artifact hashes, replay, metrics, and autoresearch ledger rows.
- Keep operating instructions grounded in tools and files actually present in this repository. Avoid carrying over tool-specific directories, fixed context-window protocols, or missing maintenance scripts unless the repo later adds them explicitly.
- Browser/screenshot verification: `chromiumfish` 0.1.3 / browser 150.0.7844 is on PATH. Its Linux cache originally unpacked without `chrome_crashpad_handler`; direct headless Chrome failed before rendering. Repaired on 2026-06-11 by copying the existing Chrome-for-Testing 142 handler from `/var/home/eturkes/debian/.local/share/choreographer/deps/chrome-linux64/chrome_crashpad_handler` into `/var/home/eturkes/debian/.cache/chromiumfish/150.0.7844/linux-x64/chromiumfish-linux-x64/`. Verified `$(chromiumfish path) --headless=new --disable-gpu --no-sandbox --window-size=1440,1000 --screenshot=/tmp/ckc-chromiumfish-mockup-repaired.png file:///run/host/home/eturkes/Projects/scratch/ckc-ui/mockups/ckc-spec04-start.html` writes a 281634-byte PNG with sha256 `b79686b5ab726c4ed05e38fe25de752e1b7cc869f210bc8def82e061109083fc`. If `chromiumfish clear` or refetch removes it, restore that handler or use a fixed upstream browser archive before visual checks.
- Codex custom prompts under `~/.codex/prompts` are user-global. Project-local command-like workflows belong under `.agents/skills`; `$session-prompt <TASK>` is a repo skill and parses the task text from the invoking message.
- 2026-06-11 reset: old mockups, `PRESENTATION.md`, and `runs/visual-checks` were deleted after SPEC.md moved to spec04 r2 M1-M6 framing. Current UI seed is the single standalone file `mockups/ckc-spec04-start.html`; it is a compact evidence workbench over §0-§15, not implementation authority or measured output.
- 2026-06-11 dashboard feedback: avoid broad dashboard chrome for CKC UI. Prefer a two-column evidence workbench with one left milestone rail and main-flow focus/run/trace/evidence panels. Keep gates, registries, spec maps, and loop ledgers off the first viewport unless the task explicitly asks for them.
- 2026-06-11 one-shot M1-M2 pass: `tools/build-run.mjs` is a deliberate JS-only deviation from spec04's Rust/model/solver stack. It regenerates ignored `runs/m2-one-shot/`, writes committed `workbench/run-data.js`, and asserts one M1 contradiction plus one documented null result. Treat it as a real fixture-scale demo artifact, not the normative compiler architecture.
- 2026-06-11 live model update: default M2 verification now uses project-local `llama.cpp` (`npm run setup:model`, ignored `.local/`) with `Qwen2.5-0.5B-Instruct` Q2_K GGUF as the weak model. `npm run verify` performs 12 live subprocess calls and rewrites `workbench/run-data.js` with live results; `npm run verify:recorded` is only a fallback and rewrites the snapshot back to the old deterministic stub.
