# Agent Memory

- Current project focus: no browser UI. Work from canonical run artifacts and tracked manuscript figures under `figures/manuscript/`; root `index.html` is obsolete, ignored, and should not be regenerated unless the user explicitly restores UI work.
- `tools/build-run.mjs` is a deliberate JS-only deviation from spec04's Rust/model/solver stack. It regenerates ignored `runs/m2-one-shot/`, writes reports/metrics/trace/replay artifacts, and asserts one M1 contradiction plus one documented null result. Treat it as fixture-scale demo evidence, not the normative compiler architecture.
- Current M2 one-shot route target: `route.direct_smt` model output is SMT-LIB; `route.single_ir` model output is bounded cue JSON, bridged into contrived `schema.route_rule_ir.v0`, then deterministically compiled to per-row SMT-LIB under `runs/m2-one-shot/route_targets/route.single_ir/`. `metrics/route_targets.json` is the durable route-target summary.
- 2026-06-11 R3 less-leaky M2 rerun: `route.single_ir` prompts no longer include the filled JSON answer object or per-label normalized field payloads. M2 scoring uses 3 groups (`group.m1_conflict`, `group.m1_null`, `group.m2_holdout_conflict`) x 3 seeds x 2 routes = 18 live calls, with `metrics/route_evaluation.json` documenting `source_derived_route_pair_evaluator.v2`. The harder R3 live run is a scaffolded cue-translation null result for lift: direct SMT admitted accuracy `0/9`; single-IR admitted accuracy `0/9`; single-IR candidate accuracy `3/9`.
- Default local model for live verification is project-local `llama.cpp` with `Qwen2.5-0.5B-Instruct-Q2_K` (`sha256 9ee36184e616dfc7...`) in ignored `.local/`. `npm run verify` runs live model calls; `npm run verify:recorded` is unsupported-model fallback evidence and changes ignored run outputs.
- Prompt provenance convention: `tools/build-run.mjs` writes exact LLM prompt text to `runs/m2-one-shot/prompts/**/prompt-<hash>.txt`, indexes prompts in `runs/m2-one-shot/prompts/catalog.json`, and adds `prompt_hash` to each `model_io/**` record. Use this catalog for prompt audits before scanning all model I/O files.
- Model-facing prompt realism: prompts passed to LLMs should frame tasks as hospital CDS knowledge-base maintenance/import or formalization tickets. Keep route IDs, research-harness language, weak-model labels, synthetic-fixture labels, and measurement mechanics in artifacts/reports, not in the prompt text sent through `llama.cpp -p`.
- 2026-06-11 R1 data spine: fixture regions, terminology bindings, clinical statements, and NormRule specs live in `corpus/fixtures/m1_fixture_semantics.json`; M1 group membership comes from `registry/experiments.json`; expected outcomes come from `corpus/gold/m1_expected.json`. Keep future fixture semantics data-driven there instead of adding `a`/`b`/`control` branches to `tools/build-run.mjs`.
- 2026-06-11 R2 real-source candidate path: `tools/build-run.mjs` emits candidate-only real guideline artifacts under ignored `runs/m2-one-shot/real_guidelines/artifacts/<source-id>/{source_graph,segments,normalization,route_rule_ir}.json` from `corpus/real_guidelines/japanese_guidelines.json`. These rows are `source_intake_candidate_only`, outside locked M1/M2 scoring; registry `machine_hint` values are retained as author-provided residual evidence, with unsupported/missing/ambiguous toy-schema fields blocking candidate rule admission.
- `corpus/real_guidelines/japanese_guidelines.json` is the committed source/permission/span registry; `npm run fetch:guidelines` downloads raw J-STAGE HTML/PDF bytes into ignored `corpus/raw/real-guidelines/`.
- Manuscript figures: `tools/build-figures.mjs` reads `runs/m2-one-shot/report.json` and writes tracked ArXiv-oriented SVG/PDF pairs plus `figures.tex`, `README.md`, `manifest.json`, and deterministic multi-page `manuscript_figures.pdf` under `figures/manuscript/`. Rebuild figures after any run regeneration; keep captions honest about the current R3 result: target-syntax lift only, no admitted lift.
- `npm run verify:figures` checks the figure bundle is a PDF and has one `/Page` object per figure.
- Keep operating instructions grounded in tools and files actually present in this repository. Avoid carrying over tool-specific directories, fixed context-window protocols, or missing maintenance scripts unless the repo later adds them explicitly.
- Codex custom prompts under `~/.codex/prompts` are user-global. Project-local command-like workflows belong under `.agents/skills`; `$session-prompt <TASK>` is a repo skill and parses the task text from the invoking message.
- No-argument `$session-prompt` asks the user for a task before changing files. The completed R1-R3 realism queue remains in `.agent/session-plan.md` as historical state, but the project-local session skill no longer consumes it.
