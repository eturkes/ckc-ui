# Agent Memory

- CKC UI framing: present a research/evidence workbench over canonical artifacts, not a clinical UI. Primary surfaces are run state, findings/null results, lineage from source span to solver verdict, artifact hashes, replay, metrics, and autoresearch ledger rows.
- Keep operating instructions grounded in tools and files actually present in this repository. Avoid carrying over tool-specific directories, fixed context-window protocols, or missing maintenance scripts unless the repo later adds them explicitly.
- Browser/screenshot verification: `chromiumfish` 0.1.3 / browser 150.0.7844 is on PATH. Its Linux cache originally unpacked without `chrome_crashpad_handler`; direct headless Chrome failed before rendering. Repaired on 2026-06-11 by copying the existing Chrome-for-Testing 142 handler from `/var/home/eturkes/debian/.local/share/choreographer/deps/chrome-linux64/chrome_crashpad_handler` into `/var/home/eturkes/debian/.cache/chromiumfish/150.0.7844/linux-x64/chromiumfish-linux-x64/`. Current UI screenshot target is root `index.html`.
- Codex custom prompts under `~/.codex/prompts` are user-global. Project-local command-like workflows belong under `.agents/skills`; `$session-prompt <TASK>` is a repo skill and parses the task text from the invoking message.
- 2026-06-11 UI cleanup: old `mockups/` and multi-file `workbench/` UI artifacts were removed. The only committed UI artifact is root `index.html`, a basic single-file report generated by `tools/build-run.mjs`.
- 2026-06-11 one-shot M1-M2 pass: `tools/build-run.mjs` is a deliberate JS-only deviation from spec04's Rust/model/solver stack. It regenerates ignored `runs/m2-one-shot/`, writes committed root `index.html`, and asserts one M1 contradiction plus one documented null result. Treat it as a real fixture-scale demo artifact, not the normative compiler architecture.
- 2026-06-11 live model update: default M2 verification uses project-local `llama.cpp` (`npm run setup:model`, ignored `.local/`) with official `Qwen2.5-1.5B-Instruct` Q4_K_M GGUF (`sha256 6a1a2eb6d15622bf3c96857206351ba97e1af16c30d7a74ee38970e434e9407e`). `route.single_ir` is source-local and grammar-constrained, but no longer receives full deterministic evidence flags; it receives each source span, exception text, and a literal primary-direction cue, then emits one CKC IR row.
- 2026-06-11 harder M2 live verification: `npm run verify` completed with 18 local model calls on `Qwen2.5-1.5B-Instruct-Q4_K_M` (`sha256 6a1a2eb6d15622bf...`). Direct SMT stayed malformed (`0/6` syntax/admission/accuracy, diagnostics `target_parse_error` + `ai_schema_violation`). Harder source-local `route.single_ir` stayed JSON-valid (`6/6`) but produced no admitted rows (`0/6`) because grounding rejected hallucinated fields; candidate verdict accuracy was `3/6`, exposing null rows as candidate-correct audit data only. Root `index.html` is regenerated from `runs/m2-one-shot/`.
- 2026-06-11 optimized direct SMT rerun: prompt-only direct SMT template plus greedy decoding makes `route.direct_smt` score `6/6` target syntax/admission/admitted accuracy and `2/2` stability on locked M2 fixtures. This supersedes the malformed-direct baseline as comparison evidence. Residual audit still records `0/6` exact template matches, `6/6` missing named assertions, and `6/6` negated-sepsis assertions, so future IR work should target source-grounded admission/trace completeness/harder fixtures rather than claiming lift from the old direct 0/6.
- 2026-06-11 real Japanese guideline intake: `corpus/real_guidelines/japanese_guidelines.json` is the committed source/permission/span registry; `npm run fetch:guidelines` downloads raw J-STAGE HTML/PDF bytes into ignored `corpus/raw/real-guidelines/`. `tools/build-run.mjs` emits `runs/m2-one-shot/real_guidelines/source_intake.json` and renders it as source-intake candidate evidence only, outside locked M1/M2 scoring and without clinical claims.
- 2026-06-11 fair weak-model lift rerun: default local model is restored to `Qwen2.5-0.5B-Instruct-Q2_K` (`sha256 9ee36184e616dfc7...`). `route.direct_smt` and `route.single_ir` now share `metrics/source_cues.json` (`lexical_cue_v1` raw+resolved source cues). Direct receives the same resolved cue rows but must compose SMT-LIB directly; single-IR copies one resolved cue field per grammar-constrained short hop, then a deterministic bridge scores verdicts. Live `npm run verify` records 60 model calls: direct SMT `0/6` syntax/admission/admitted accuracy, single-IR `6/6` syntax/admission/admitted accuracy, stability delta `0/2 -> 2/2`. This is a stronger IR-help signal than the old malformed direct run, but the caveat is explicit: the IR route benefits from a deterministic lexical cue layer shared with direct, so future claims should call this a shared-cue short-hop lift, not raw Japanese understanding.
