# Agent Memory

- Current presentation focus: root `index.html` is the tracked static browser UI. It was restored from `b998af4^`, then adapted on 2026-06-11 into a SPEC-r3 M3 evidence dashboard over ignored `runs/m3-routes`, `runs/m3-compare`, and `runs/m3-coverage` artifacts. `npm run verify:recorded` still defaults to `exp.m2_shorthop`; verify M3 UI numbers with direct artifact-consistency checks, not that gate alone. Manuscript figure artifacts and `tools/build-figures.mjs` remain absent and recoverable from Git history.
- `tools/build-run.mjs` is a deliberate JS-only deviation from spec04's Rust/model/solver stack. It regenerates ignored `runs/m2-one-shot/`, writes reports/metrics/trace/replay artifacts, and asserts one M1 contradiction plus one documented null result. Treat it as fixture-scale demo evidence, not the normative compiler architecture.
- Current M2 one-shot route target: `route.direct_smt` model output is SMT-LIB; `route.single_ir` model output is bounded cue JSON, bridged into contrived `schema.route_rule_ir.v0`, then deterministically compiled to per-row SMT-LIB under `runs/m2-one-shot/route_targets/route.single_ir/`. `metrics/route_targets.json` is the durable route-target summary.
- 2026-06-11 R3 less-leaky M2 rerun: `route.single_ir` prompts no longer include the filled JSON answer object or per-label normalized field payloads. M2 scoring uses 3 groups (`group.m1_conflict`, `group.m1_null`, `group.m2_holdout_conflict`) x 3 seeds x 2 routes = 18 live calls, with `metrics/route_evaluation.json` documenting `source_derived_route_pair_evaluator.v2`. The harder R3 live run is a scaffolded cue-translation null result for lift: direct SMT admitted accuracy `0/9`; single-IR admitted accuracy `0/9`; single-IR candidate accuracy `3/9`.
- Default local model for live verification is project-local `llama.cpp` with `Qwen2.5-0.5B-Instruct-Q2_K` (`sha256 9ee36184e616dfc7...`) in ignored `.local/`. `npm run verify` runs live model calls; `npm run verify:recorded` is unsupported-model fallback evidence and changes ignored run outputs.
- Prompt provenance convention: `tools/build-run.mjs` writes exact LLM prompt text to `runs/m2-one-shot/prompts/**/prompt-<hash>.txt`, indexes prompts in `runs/m2-one-shot/prompts/catalog.json`, and adds `prompt_hash` to each `model_io/**` record. Use this catalog for prompt audits before scanning all model I/O files.
- Model-facing prompt realism: prompts passed to LLMs should frame tasks as hospital CDS knowledge-base maintenance/import or formalization tickets. Keep route IDs, research-harness language, weak-model labels, synthetic-fixture labels, and measurement mechanics in artifacts/reports, not in the prompt text sent through `llama.cpp -p`.
- 2026-06-11 R1 data spine: fixture regions, terminology bindings, clinical statements, and NormRule specs live in `corpus/fixtures/m1_fixture_semantics.json`; M1 group membership comes from `registry/experiments.json`; expected outcomes come from `corpus/gold/m1_expected.json`. Keep future fixture semantics data-driven there instead of adding `a`/`b`/`control` branches to `tools/build-run.mjs`.
- 2026-06-11 R2 real-source candidate path: `tools/build-run.mjs` emits candidate-only real guideline artifacts under ignored `runs/m2-one-shot/real_guidelines/artifacts/<source-id>/{source_graph,segments,normalization,route_rule_ir}.json` from `corpus/real_guidelines/japanese_guidelines.json`. These rows are `source_intake_candidate_only`, outside locked M1/M2 scoring; registry `machine_hint` values are retained as author-provided residual evidence, with unsupported/missing/ambiguous toy-schema fields blocking candidate rule admission.
- `corpus/real_guidelines/japanese_guidelines.json` is the committed source/permission/span registry; `npm run fetch:guidelines` downloads raw J-STAGE HTML/PDF bytes into ignored `corpus/raw/real-guidelines/`.
- Agent OS (SPEC §1-§2): `/session-prompt` + `/codex-review` live in `.claude/commands/`; `.agent/roadmap.md` is the build plan, this file the lessons, `.agent/compaction.sh` the context gauge. Retired the Codex-era OS (`AGENTS.md`, `.agent/session-prompt.md`, `.agents/skills/` `$session-prompt`, and the `.agent/session-plan.md` C1-C8 queue; its outcomes survive in the route lines below). Do-not-read = `.claude/settings.json` Read-denies for `.local/` (model weights) and `corpus/raw/` (scrape bytes); both gitignored so Serena already skips them and `ignored_paths` stays `[]`. `tools/build-run.mjs` is 8070 LoC of real code: navigate via Serena symbols / offset reads, not whole-file.
- 2026-06-11 C1 route-matrix migration: `tools/build-run.mjs` emits `metrics/route_matrix.json` / `report.metrics.route_matrix`; `route.direct_smt` is the explicit baseline and result classifications use baseline-delta vocabulary. `metrics/route_targets.json` / `report.route_target_summary` is schema `route_target_summary.v1` with `routes[]`, `total_compiled_row_count`, and `total_smt_file_count`; future routes should add compiled targets by route, not by replacing a single `route.single_ir` summary.
- 2026-06-11 C2 scaffold/current M2 name: `tools/build-run.mjs` defaults to `exp.m2_shorthop` while preserving run directory `runs/m2-one-shot`; select M3 routes with `--experiment exp.m3_routes`. Any registered route not listed in `implementedRouteIds` fails closed unless `--scaffold-routes` is explicit. In scaffold mode it emits `measurement_status: scaffold_closed_unimplemented`, `model_call_recorded: false`, `deferred_gate_required`, zero prompt-catalog calls, and no subprocess.
- 2026-06-11 C3 stacked route: `route.stacked_ir` is implemented in `tools/build-run.mjs` as one schema-constrained model call per source pair using `schema.stacked_ir.v0` (`source_frame -> rule_row`) and deterministic `stacked_ir_v0_to_route_rule_ir_v0` bridge into the existing `route_rule_ir_v0_to_smt_v0` compiler. C3 live `exp.m3_routes --scaffold-routes` verification had model-output syntax `9/9`, target syntax `9/9`, candidate accuracy `3/9`, admitted accuracy `0/9`, compiled rows `9`, SMT files `12`, and bridge schema `schema.stacked_ir.v0`.
- 2026-06-11 C4 hop-chain route: `route.ir_hop_chain` is implemented in `tools/build-run.mjs` as three adjacent constrained JSON calls per route row: lexical cues (`schema.ir_hop_chain.lexical_cues.v0`) -> clinical frame (`schema.ir_hop_chain.clinical_frame.v0`) -> rule rows (`schema.ir_hop_chain.rule_rows.v0`), bridged by `ir_hop_chain_v0_to_route_rule_ir_v0` into the existing SMT compiler. C4 live `exp.m3_routes --scaffold-routes` verification had model-output syntax `9/9`, target syntax `9/9`, candidate accuracy `9/9`, admitted accuracy `0/9`, compiled rows `9`, SMT files `15`, `model_call_count` `27`, and hop lineage on every model_io row. Admission was blocked by grounding/bridge residuals, so this was target-syntax/candidate-verdict evidence only.
- 2026-06-11 C5/CKC-layered current implementation: `route.ckc_layered` now runs one constrained JSON call per source label per CKC stage: segments (`schema.ckc_layered.segments.v0`) -> statements (`schema.ckc_layered.statements.v0`) -> rules (`schema.ckc_layered.rules.v0`). The bridge remains `ckc_layered_v0_to_route_rule_ir_v0`; `model_io` stage lineage records `source_label` for each stage call. This replaced the original pair-level three-call route because it completed only one label per row and produced `0/24` target syntax.
- 2026-06-11 C6/spec-r3 fixture expansion: `exp.m3_routes` has 10 configured evaluation groups: 2 original M1, 1 M2 holdout, 2 M3 metamorphic, and 5 M3 expanded groups (`numeric_threshold_empty_intersection`, `strict_factual_contradiction`, `terminology_incoherence`, `condition_unsatisfiable`, `exception_resolved_conflict`). Synthetic fixture labels now extend through I/J; I carries an internally unsatisfiable age guard and J is the renal-severe counterpart for A's exception-resolved raw-overlap case. `tools/build-run.mjs` emits `metrics/group_audit.json` / `report.m3_group_audit`; group audit must cover all configured groups even when route trials are sampled smaller for iteration.
- 2026-06-11 C7/spec-r3 deterministic comparison: `exp.m3_compare` selects `pipe.direct_rule_to_smt` as baseline and `pipe.one_shot_js_ckcir_to_smt` as layered comparator over the 10 M3 groups without model calls. `node tools/build-run.mjs --verify --recorded-model --experiment exp.m3_compare` writes ignored `runs/m3-compare/{candidate_diff.json,component_reuse_graph.json,compactness_front.json,metrics/pipeline_matrix.json}` and passes with both pipelines at verdict accuracy `10/10` and conflict-kind accuracy `10/10`; current layered-minus-direct deltas are `0/1` for compile/verdict/conflict-kind accuracy and component reuse remains reported separately as a baseline delta.
- 2026-06-11 quick-iteration M3 route mode: `exp.m3_routes` keeps all 10 configured evaluation groups in `route_experiment.evaluation_group_ids`, `route_evaluation.evaluation_groups`, and `m3_group_audit`, but measures explicit `trial_group_ids` `[group.m1_conflict, group.m1_null, group.m2_holdout_conflict, group.m3_threshold_conflict]` with `sample_seeds [11]`, so route denominators are 4 rows per route. Latest live `node tools/build-run.mjs --verify --live-model --experiment exp.m3_routes` passed with 48 model calls; admitted accuracy stayed `0/4` for all five routes; candidate accuracy direct `0/4`, single_ir `1/4`, stacked_ir `1/4`, ir_hop_chain `3/4`, ckc_layered `1/4`.
- 2026-06-11 spec-r3 final evidence pass: route and pipeline runs emit root-level `ranking.csv` and `score_breakdown.json`; route ranking is lexicographic over admitted accuracy, admission, target syntax, and seed stability, with candidate accuracy retained as audit-only. `exp.m3_coverage` is implemented as a deterministic no-model run over build/apply groups and currently verifies `component_coverage_rate 35/36`, verdict accuracy `3/3`, conflict-kind accuracy `3/3`, and apply-phase model calls `0`.
- 2026-06-11 route-ranking UI convention: root `index.html` is now a pruned single-purpose Route Ranking page with English first and a Japanese mirror below it. The UI avoids "M3" wording and ranks by accuracy from `candidate_verdict_accuracy` (`ir_hop_chain` 3/4; `single_ir`/`stacked_ir`/`ckc_layered`/`ckc_repair` 1/4; `direct_smt` 0/4). Main table order is success rank, complexity rank, route, accuracy, route description; keep schema ids such as `route_rule_ir.v0` and unexplained CKC terminology out of this table unless explicitly requested. Describe `ckc_layered` as an in-development project-native IR route, `ckc_repair` as that route plus a self-repair pass.
- 2026-06-19 S1 `route.ckc_repair` (first of four sophisticated IR routes; roadmap Active initiative): `route.ckc_layered` base + bounded rule-stage self-repair loop (`ckcRepairMaxIterations=2`) in `tools/build-run.mjs`. Per label that still carries residuals it re-runs stage3.rules with feedback built from intrinsic residuals (`ckcRepairFeedbackLines`, input_artifact kind `ckc_repair_feedback`), adopting an attempt only if `ckcRepairAttemptIsBetter` (sort: target_syntax > model_output_syntax > fewer actionable residuals; ties keep incumbent). Feedback and selection are answer-agnostic: gold-coupled FP/FN residual codes are excluded via `ckcRepairActionableResiduals`, so the loop never reads `expected`. Reuse surface for S2-S4: `implementedRouteIds`, the `llamaArgs` branch shared with ckc_layered, `runLiveRoute` dispatch -> `runLiveCkcRepairRoute` reusing `classifyCkcLayeredCandidate`, `registry/routes.json` entry, `exp.m3_routes.routes`, EN+JA `index.html` row. Every repair call is pushed to `modelCalls` for honest accounting even when not adopted; `parsed_response.repair_trace` records per-iteration `{kind, target_syntax_valid, model_output_syntax_valid, actionable_residual_codes, adopted}`.
- 2026-06-19 S1 measured result (live `exp.m3_routes`, 4 trial rows, seed 11): 80 total model calls; ckc_repair contributed 32 (24 base ckc_layered-equivalent + 8 repair, 2/group). candidate accuracy 1/4, admitted 0/4 -- identical to ckc_layered, so harness `ranking.csv` puts the two adjacent (ckc_layered rank 1, ckc_repair rank 2 on lexicographic tiebreak). Every repair attempt was rejected (`adopted:false`): on the Q2_K 0.5B model revision degraded well-formedness (target_syntax_valid true->false, actionable residuals 3-4 -> 7), so the comparator fell back to base every time. Honest null lift -- bounded self-repair adds no candidate accuracy here because re-drafting this weak model breaks structure faster than it fixes grounding; durable value is the safe-fallback + answer-agnostic-feedback infra, reusable by S4 `route.ckc_cegis`. UI surfaces ckc_repair at success rank 2 (1/4, amber 25%), complexity rank 6 (most complex), inserted after ckc_layered in both tables.
- 2026-06-19 index.html config-pipeline figure: below each locale's ranking table sits a `Route pipelines` figure (classes `.configs`/`.config-row`/`.config-head`/`.pipeline`/`.node`/`.edge`/`.loop`; new `--accent #3f6184`), one card per route ordered by complexity rank (direct_smt -> single_ir -> stacked_ir -> ir_hop_chain -> ckc_layered -> ckc_repair). Nodes are artifacts (`Source` / intermediate drafts / `Solver target`, endpoints bold); edges encode the transform: solid accent arrow = one model generation, dashed muted arrow (`.edge.compile`) = deterministic compile. direct_smt has a model edge and NO compile edge (model writes SMT directly); every other route compiles the last draft. ckc_repair's Rules node carries a `↻ ×2` loop badge (`ckcRepairMaxIterations`). Each card head mirrors the table accuracy dot+ratio (red 0/4, amber 1/4, green 3/4); figure adds no new measured numbers. CSS-only, no JS; decorative arrows/glyphs are aria-hidden; full EN+JA parity (JA node labels ソース/構造化ドラフト/積層ドラフト/語彙の手がかり/臨床フレーム/ルール行/セグメント/ステートメント/ルール/ソルバーターゲット). Pipelines `overflow-x:auto` inside their card on narrow widths. UI-verify recipe: headless render with the chromiumfish-managed chrome (`"$(chromiumfish path)" --headless --no-sandbox --force-device-scale-factor=2 --window-size=W,H --screenshot=out.png file://$PWD/index.html`); no PIL/imagemagick on host, so crop regions with `uv run --with pillow python`.
- 2026-06-19 index.html accuracy clarity pass: (1) added an explicit `Accuracy` definition paragraph (count over 4 fixed scenarios; per scenario the route drafts a rule -> compiles to solver target -> solver verdict `conflict`/`no conflict` is checked vs the scenario's expected answer; one run per scenario at a fixed seed). (2) Retired the opaque continuous bar (`.ratio`/`.track`/`.fill`); the ranking-table Accuracy cell is now a 4-pip strip (`.acc`/`.pips`/`.pip`) ordered by the 4 trial scenarios `[group.m1_conflict, group.m1_null, group.m2_holdout_conflict, group.m3_threshold_conflict]` -- filled pip = pass, tier-colored (green 3/4, amber 1/4), empty = fail; `.acc code` needs `white-space:nowrap;overflow-wrap:normal` to beat the global `code{overflow-wrap:anywhere}` that split `3/4`. (3) Added a supplementary per-scenario matrix (`<table class="matrix">`, `.m.pass.{green,amber,red}` tinted cells + `.col-exp` expected-verdict sublabels, `&#10003;`/`&middot;` glyphs with `aria-label`): route x those 4 scenarios. Cells mirror `candidate_verdict_correct` per row from `runs/m3-routes/metrics/raw_rows.json` (counts cross-checked vs `runs/m3-routes/ranking.csv`). Honest pattern it exposes (was hidden by the bar): the four routes tied at 1/4 pass ONLY the no-conflict control `group.m1_null`; `ir_hop_chain` adds `m1_conflict`+`m2_holdout_conflict`; `m3_threshold_conflict` fails for every route; `direct_smt` 0/4. Full EN+JA parity (matrix scenario headers EN Contradiction/Control/Reworded contradiction/Numeric-threshold conflict, JA 矛盾/対照/言い換えた矛盾/数値しきい値の衝突, expected-verdict sublabels 衝突を期待・衝突なしを期待; the "reworded contradiction" column is `group.m2_holdout_conflict` -- the same recommend-vs-contraindicate conflict via a held-out source `fixture.m2_guideline_d` with new wording). Per-route UI surface is now 3 places x 2 locales: ranking-table row+pips, matrix row, pipeline card -- future routes (S2-S4) update all three EN+JA. Source of truth for these cells: `runs/m3-routes/ranking.csv` (counts) + `metrics/raw_rows.json` (per-cell verdict correctness).
