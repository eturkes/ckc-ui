{
  "routes": [
    {
      "id": "route.direct_smt",
      "shape": "weak local model emits SMT-LIB text directly",
      "runtime": "project-local llama.cpp subprocess via tools/setup-local-model.mjs",
      "default_model": "Qwen2.5-0.5B-Instruct Q2_K GGUF",
      "live_model": true
    },
    {
      "id": "route.single_ir",
      "shape": "weak local model fills a compact CKC IR row schema, then deterministic scoring checks admission",
      "runtime": "project-local llama.cpp subprocess via tools/setup-local-model.mjs",
      "default_model": "Qwen2.5-0.5B-Instruct Q2_K GGUF",
      "live_model": true
    }
  ]
}
